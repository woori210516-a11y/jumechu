import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'momechu',
  brand: {
    primaryColor: '#FF5C00',
  },
  permissions: [],
  webBundleDir: 'out',
});
